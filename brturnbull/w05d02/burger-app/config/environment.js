const databaseURI = 'mongodb://localhost/burgers';
const port = 8000;

module.exports = {databaseURI, port};

//setting up the environment in configuration
//what post and database are we using? - then export to be used in the other files
